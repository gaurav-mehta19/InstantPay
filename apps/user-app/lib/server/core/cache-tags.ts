export const balanceTag = (userId: string) => `balance:${userId}`;
export const transactionsTag = (userId: string) => `transactions:${userId}`;
export const profileTag = (userId: string) => `profile:${userId}`;
